kind: Ingress
apiVersion: extensions/v1beta1
metadata:
  name: "foo"

spec:
  rules:
    - host: example.com
      http:
        paths:
          - path: /bar
            backend:
              serviceName: whoami
              servicePort: 80
